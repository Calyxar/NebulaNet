rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Dev rule: only signed-in users can read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
